// Tarea 1:
// Preguntarle al usuario su nombre.
// Si el nombre del usuario es el mismo que  el  de ustedes
// Imprimir "Hola, Tocayo! Yo también me llamo " y su nombre.
// Elijan otro nombre, puede ser de un pariente, amigo, conocido.
// Si el nombe del usuario es el mismo que el que nombre que eligieron
// Imprimir "Hola " y el nombre, " te llamás igual que mi ..."
// Si no, simplemente imprimir "Hola " + nombre!

/*const nombreDelUsuario = prompt("¿Cual es tu nombre?");
let respuesta;
if(nombreDelUsuario.toLowerCase() === "lautaro"){;
    respuesta = 'genial mi nombre tambien es ' + nombreDelUsuario ;
}else if(nombreDelUsuario.toLowerCase() === "paolo"){;
    respuesta = "hola " + nombreDelUsuario + ", te llamas igual que mi primo";
}else{
    respuesta = "hola " + nombreDelUsuario;
}
console.log(respuesta);
*/

//Tarea 2:
// Preguntar la edad del usuario
// Hacerle saber si tiene más, menos ó la misma edad que nosotros.

/*const edadDelUsuario = Number(prompt("¿Cuantos años tenes?"))
let respuesta;
if(edadDelUsuario === 15){;
    respuesta = "genial tenemos la misma edad";
}else if(edadDelUsuario > 15){;
    respuesta = "mira vos che sos mayor que yo";
}else if(edadDelUsuario < 15){;
    respuesta = "ahhh asi que sos mas chico que yo";
}else{;
    respuesta = "eso ni es un numero crack"
};
console.log(respuesta);
*/

//Tarea 3:
// Preguntarle al usuario si tiene documento, y que conteste con "si" o "no".
// Si dice si, preguntarle la edad.
// Si la edad es mayor a 18, dejarlo entrar al bar.
// Si la edad es menor a 18, no dejarlo entrar al bar.
// Si no tiene documento, no dejarlo entrar al bar.
// Si no entendemos la respuesta, le decimos que no entendimos la respuesta.
// Punto bonus: SI, NO, Si, No, si, no.

/*const documentoDelUsuario = prompt("¿tiene docuemnto?");
let respuesta;
if(documentoDelUsuario.toLowerCase() === "si"){;
    const edadDelUsuario = Number(prompt("¿cuantos años tenes?"));
    if(edadDelUsuario >= 18){;
        respuesta = "Puede pasar el bar";
    }else if(edadDelUsuario < 18){;
        respuesta = "No puede pasar al bar";
    }else{;
        respuesta = "Basta de chistes, decime cuantos años tenes ";
    };
}else if(documentoDelUsuario.toLowerCase() === "no"){;
    respuesta = "Disculpe no puede pasar al bar";;
}else {;
    respuesta = "Tenes o no documento";
};
console.log(respuesta);
*/







