/*function probarValidarSalarios(){
    console.assert(
        validarSalarios("") === "este campo no puede ser vacio",
        "validarSalarios no valido que el salario no fuera vacio"
    )
    console.assert(
        validarSalarios(0) === "este campo no puede ser 0",
    "validarSalario no valido que el salario no sea 0"
    )
    console.assert(
        validarSalarios("hola") === "este campo solo puede tener numeros",
        "validarSalario no valido que el salario fueran numeros"
    )
}
probarValidarSalarios()
*/